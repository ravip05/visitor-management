# Example environment configuration for VMS backend

# Server settings
PORT=4000
NODE_ENV=development

# JWT secret key (for signing tokens)
JWT_SECRET=my_super_secret_dev_key_123456789

# SQLite database file path
# - Can be absolute (C:\Users\parma\visitor\vms.sqlite) 
# - Or relative to vms-backend folder (./vms.sqlite)
DB_FILE=../vms-backend/vms.sqlite

# Optional: logging level (info, debug, error)
LOG_LEVEL=info

# Optional: CORS origin (default allows all if not set)
# CORS_ORIGIN=http://localhost:5173
